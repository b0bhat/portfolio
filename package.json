{
  "dependencies": {
    "gh-pages": "^6.1.1",
    "three": "^0.157.0"
  },
  "devDependencies": {
    "esbuild-wasm": "^0.19.4"
  },
  "scripts": {
	"predeploy": "npm run build",
    "deploy": "gh-pages -d dist -b gh-pages"
  }
}
